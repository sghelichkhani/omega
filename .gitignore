# Byte-compiled / cache directories
*__pycache__*
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Build and install artefacts
build/
dist/
*.egg-info/

# macOS
.DS_Store

# Simulation output. Every one of these is regenerated by the demo or test
# that writes it, and they run to hundreds of megabytes, so they stay out of
# the history.
*.h5
*.pvd
*.vtu
*.tif

# Working notes -- private research record, never committed
NOTES/

# Local agent/editor instruction files
CLAUDE.md
AGENTS.md
